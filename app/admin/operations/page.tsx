import { OperationsCommandCenter } from "@/components/admin/operations-command-center";

export const metadata = {
  title: "Operations Command Center | VI Guide",
  description: "Monitor live VI Guide booking demand, traveler volume, merchant responses, and operational alerts.",
};

export default function OperationsPage() {
  return <OperationsCommandCenter />;
}
