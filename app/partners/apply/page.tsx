import { PartnerApplicationForm } from "@/components/partners/partner-application-form";

export const metadata = {
  title: "Become a VI Guide Partner",
  description:
    "Apply to operate a VI Guide business listing, receive booking requests, and participate in concierge referrals across the U.S. Virgin Islands.",
};

export default function PartnerApplicationPage() {
  return <PartnerApplicationForm />;
}
