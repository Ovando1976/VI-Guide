# Refined map markers

Normal markers are compact 30px dimensional category jewels. Only the selected
location expands into a 42x50 teardrop. This reduces satellite-map obstruction
while preserving clear category recognition and touch feedback.

```bash
node scripts/install-refined-map-icons.mjs
node scripts/install-refined-map-icons.mjs --apply
npm run typecheck
```
