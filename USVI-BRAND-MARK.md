# USVI seal brand mark

Replaces the yellow text-only `VI` tile with the existing official-looking
seal asset at `/images/usvi-logo.jpeg` on the homepage, persistent navigation,
and map loading state.

```bash
node scripts/install-usvi-brand-mark.mjs
node scripts/install-usvi-brand-mark.mjs --apply
npm run typecheck
```
